---
description: Generate TypeScript interfaces and types
---

Generate TypeScript interfaces and types from the current context.

Include:

1. Domain models and value objects
2. Persistence row, create, update, and query DTO types
3. API request/response types
4. UI props, view models, state, and data-flow notes
5. Assumptions, mapping notes, and review checklist

Represent nullability, optional fields, IDs, dates, decimals, enums, and lifecycle states
explicitly.
